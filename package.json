{
  "name": "carbon-footprint-app",
  "version": "1.0.0",
  "description": "A simple web application to calculate the digital carbon footprint of an individual.",
  "main": "src/script.js",
  "scripts": {
    "start": "live-server src",
    "build": "tsc && cpx src/*.html dist && cpx src/*.css dist && cpx src/*.js dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "gh-pages": "^6.3.0",
    "live-server": "^1.2.1",
    "typescript": "^4.5.0"
  },
  "author": "",
  "license": "ISC"
}
